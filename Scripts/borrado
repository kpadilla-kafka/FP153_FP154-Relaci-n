USE [AuraPortal_BPMS_DES];
GO

BEGIN TRAN;

DELETE rel
FROM [dbo].[AP__FamilyRelational_Data] rel
INNER JOIN [dbo].[AP_Dyn_Familias_154] f154
    ON f154.ID = rel.ElementFamily2Id
WHERE rel.RelationId = 25
  AND f154.[1_VUI - Procesos/Name] = 20;

SELECT @@ROWCOUNT AS RelacionesEliminadas;

-- Si el número coincide con 8:
COMMIT;

-- Si algo se ve raro, en vez de COMMIT ejecutar:
-- ROLLBACK;

Perfecto, Kati. Salió exactamente lo esperado: 8 relaciones eliminadas.

BEGIN TRAN;

DELETE FROM [dbo].[AP_Dyn_Familias_154]
WHERE [1_VUI - Procesos/Name] = 20;

SELECT @@ROWCOUNT AS Ramas154Eliminadas;

-- Si devuelve 20:
COMMIT;

-- Si algo se ve raro:
-- ROLLBACK;
-----------------

BEGIN TRAN;

DELETE FROM [dbo].[AP_Dyn_Familias_154]
WHERE [1_VUI - Procesos/Name] = 20;

SELECT @@ROWCOUNT AS Ramas154Eliminadas;

-- Si devuelve 20:
COMMIT;

-- Si algo se ve raro:
-- ROLLBACK;

devuelve 20


Ya tenemos:

Backup 153: 27 registros
Backup 154: 731 registros
Backup relaciones trámite 20: 8 registros
Relaciones trámite 20 eliminadas: 8
Ramas 154 trámite 20 eliminadas: 20

Ahora sí tenemos limpia la familia 154 para el trámite 20.
